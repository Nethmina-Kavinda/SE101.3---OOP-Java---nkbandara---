public class MyFirstInterface 
{
    final int x=0;
    // without static final keyword no problem because java get default it is final keyword because it in to the interface word

    public abstract void display();
    public void display1();

    // no any change. Because java get already public  void display () is a abstract method.we can use abstract method or without abstract method  we can use it. because we creating all methods are abstract methods.
}

